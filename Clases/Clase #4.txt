Que es JAVA?

Es un lenguaje de programacion creado en 1995
Tiene una sintaxis similar a la de C y C++
Una de sus principales caracteristicas es su portabilidad

Que se puede programar?

Casi cualquier cosa que se necesite:
	Apps graficas de alta calidad
	Operaciones de SQL sobre base de datos
	Aplicaciones web y de red

Es muy facil encontrar bibliotecas ya que por su antigüedad y por ser unos de los mas utilizados,
hay mucha informacion de facil acceso

-Operadores:

Aritmeticos:
	Suma	+
	Resta	-
	Mult	*
	Div	/
	Mod	%

Asignacion:
	op1 = op1 + op2  // op1 += op2
		  -		-=
		  *		*=
		  /		/=
		  %		%=

Relacionales:
	Mayor	>   // 	>=
	Menor	<   //  <=
	iguald	==
 	dife	!=
	
Logicos:
	&&	if(t&&t) // si(a y b)
	||	if(t||t) // si(a o b)
	!	if(!t)   // si(no sucede t)
	& // |  se utilizan en binario

Unarios:
	+ // -
	c = a + - b

Instance of:
	objeto instance of class

Incrementales:
	++	incrementa	a++
	--	decrementa	a--

Concatenacion:
	System.output.println("El total es" + resultado + "unidades")

-Crear variable

Tipo; var = intacia, tipo();

-Tipos de datos-
	byte	8-bits
	short	16-bits
	int	32-bits
	long	64-bits
	float	Punto flotante de 32-bits
	double	Punto flotante de 64-bits
	char	Caracter
	string	Union de caracteres
	boolean	True, False

-Modificador de acceso
public		-Todos pueden ver la clase
protected	-Solo los del paquete y subclase pueden tener acceso
sin modif	-Solo el paquete tiene acceso
private		-Solo la clase tiene acceso

-Estructura de control
if, else, return
	if(num<num2){
		result +=2;
	}
	else{
		result -=1;
	}
	return resultado

Si se usa Static se permite ejecutar la instancia

-While
Es un bucle que se ejcuta hasta cierta condicion, no es necesario que se ejecute
	while(r<0, 99d){

	}

-do while
Es un bucle como el while pero se ejecuta al menos una vez
	do
		"operacion"
	while("cond")

-for
Es un bucle que se ejecuta una cantidad de veces determinada(se conoce cuantas veces realiza la accion)
	for(int i = 0; i<10 : i++){

	}
